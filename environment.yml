name: basismixer

channels:
  - defaults
  - pytorch

dependencies:
  - python=3
  - numpy
  - scipy
  - matplotlib
  - ipython
  - jupyter
  - pytorch
  - pip
  - tqdm

  - pip:
    - soundfile
    - partitura
    - git+https://github.com/OFAI/basismixer.git@develop
